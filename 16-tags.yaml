- name: Print Playbook
  hosts: localhost 
  tasks:
    - name: Print the message
      ansible.builtin.debug:
        msg: "Hello DevOps"
      tags:
        - devops

    - name: Print the message
      ansible.builtin.debug:
        msg: "Hello AWS"
      tags:
        - aws